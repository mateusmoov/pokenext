import styled from "styled-components";

export const InputContainer = styled.input`
  border: none;
  border-radius: 10px;
  font-size: 24px;
  width: 825px;
  height: 59px;
  padding-left: 38px;

  &::placeholder {
    color: rgba(0, 0, 0, 0.5);
  }
`;
